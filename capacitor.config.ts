import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eventapp.app',
  appName: 'EventApp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;